# Cloudflare Worker URL for the leaderboard backend (see worker/README.md).
# Set your deployed URL here and provide the same value as a GitHub Actions
# secret named NEXT_PUBLIC_LEADERBOARD_URL. Leave empty to disable leaderboards.
NEXT_PUBLIC_LEADERBOARD_URL=https://drogan-leaderboard.<account>.workers.dev